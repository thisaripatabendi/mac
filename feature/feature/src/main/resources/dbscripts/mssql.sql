
-- -----------------------------------------------------
-- Table `mac_DEVICE`
-- -----------------------------------------------------
CREATE  TABLE IF NOT EXISTS mac_DEVICE (
  mac_DEVICE_ID VARCHAR(45) NOT NULL ,
  DEVICE_NAME VARCHAR(100) NULL DEFAULT NULL,
  PRIMARY KEY (mac_DEVICE_ID) );

